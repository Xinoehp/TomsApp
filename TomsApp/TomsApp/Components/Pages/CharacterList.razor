@using TomsApp.Models

<MudNavMenu>
	<MudCard>
		<MudText Typo="Typo.h6">Characters</MudText>
		<MudCardContent>
			<MudButton StartIcon="@Icons.Material.Filled.Create">New Character</MudButton> @* OnClick="@(() => ClearCharacterSelected())" *@
		</MudCardContent>

		@foreach (Character character in _characters)
		{
			<MudCardContent Style="display: flex;">
				<MudTextField T="string" /> @* @bind-Value="@_character.Name" *@
				<MudIconButton Icon="@Icons.Material.Filled.Delete" /> @* OnClick="@(() => _characters.Remove(_character))" *@
			</MudCardContent>

		}
	</MudCard>

</MudNavMenu>